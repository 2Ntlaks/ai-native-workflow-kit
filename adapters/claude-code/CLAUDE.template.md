# CLAUDE.md Template

- Explore first, then plan, then code for complex tasks.
- Use `templates/agent-task-brief.md`.
- Follow `docs/autonomy-levels.md`.
- Use `workflows/` for task-specific process.
- End implementation work with `templates/review-packet.md`.

## Verify Before Use

Claude-specific hooks, skills, subagents, permission modes, and memory behavior should be verified against current Claude Code docs before relying on them.
